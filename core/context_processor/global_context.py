def global_context(request):
    """
    Generate a dictionary to store the global context for each request.

    Parameters:
    - request: The request object that is passed to the function.

    Returns:
    - dict: The dictionary containing the global context.
    """

    # Create an empty dictionary to store the global context
    context = {}

    # Return the global context dictionary
    return context
